swagger: "2.0"
info:
  version: 1.0.0
  title: API Activos Informáticos
# during dev, should point to your local machine
host: localhost:10010
# basePath prefixes all resource paths
basePath: /
#
schemes:
  # tip: remove http to make production-grade
  - http
  - https
# format of bodies a client can send (Content-Type)
consumes:
  - application/json
# format of the responses to the client (Accepts)
produces:
  - application/json
paths:
  /graph:
    x-swagger-router-controller: "AssetType"
    post:
      tags:
      - "AssetType"
      summary: "Imagen de ciclo de vida para previsualizar"
      description: "Imagen del ciclo de vida de previsualizacion\n"
      operationId: "graphPreview"
      parameters:
      - in: "body"
        name: "confGraph"
        description: "Esquema graphviz."
        required: true
        schema:
          $ref: "#/definitions/LifeCycleGraph"
      responses:
        200:
          description: "Imagen de ciclo de vida"
          schema:
            $ref: "#/definitions/LifeCycleGraph"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /users:
    x-swagger-router-controller: "User"
    get:
      tags:
      - "User"
      summary: "Consulta de usuarios"
      description: "Listado de todos los usuarios autorizados a utlizar el servicio"
      operationId: "usersGet"
      parameters: []
      responses:
        200:
          description: "Listado de usuarios"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/User"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    post:
      tags:
      - "User"
      summary: "Crear usuario"
      description: "Dar de alta un usario dandole asi permiso para utlizar el servicio"
      operationId: "usersPost"
      parameters:
      - in: "body"
        name: "user"
        description: "Usuario a dar de alta en el sistema"
        required: true
        schema:
          $ref: "#/definitions/NewUser"
      responses:
        201:
          description: "Usuario creado"
          schema:
            # a pointer to a definition
            $ref: "#/definitions/NewResource"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /users/{id}:
    x-swagger-router-controller: "User"
    get:
      tags:
      - "User"
      summary: "Usuario"
      description: "Usuario por identificador\n"
      operationId: "usersIdGet"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del usuario."
        required: true
        type: "string"
      responses:
        200:
          description: "Usuario por id."
          schema:
            $ref: "#/definitions/User"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    put:
      tags:
      - "User"
      summary: "Usuario"
      description: "Usuario por identificador\n"
      operationId: "usersIdPut"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del usuario."
        required: true
        type: "string"
      - in: "body"
        name: "user"
        description: "Datos de usuario para actualizar"
        required: true
        schema:
          $ref: "#/definitions/UpdateUser"
      responses:
        200:
          description: "Usuario actualizado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    delete:
      tags:
      - "User"
      summary: "Usuario"
      description: "Usuario por identificador\n"
      operationId: "usersIdDelete"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del usuario."
        required: true
        type: "string"
      responses:
        200:
          description: "Usuario borrado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assetTypes:
    x-swagger-router-controller: "AssetType"
    get:
      tags:
      - "AssetType"
      summary: "Consulta de tipos de activos informáticos"
      description: "Listado de todos los tipos de activos informáticos disponibles"
      operationId: "atGet"
      parameters: []
      responses:
        200:
          description: "Listado de tipos de activos"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssetType"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    post:
      tags:
      - "AssetType"
      summary: "Crear tipo de activo"
      description: "Definir un nuevo tipo de activo a partir del cual se instanciarán nuevos activos informáticos."
      operationId: "atPost"
      parameters:
      - in: "body"
        name: "assetType"
        description: "Definicion del nuevo tipo de activo"
        required: true
        schema:
          $ref: "#/definitions/NewAssetType"
      responses:
        201:
          description: "Tipo de activo creado"
          schema:
            # a pointer to a definition
            $ref: "#/definitions/NewResource"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assetTypes/{id}:
    x-swagger-router-controller: "AssetType"
    get:
      tags:
      - "AssetType"
      summary: "Tipo de activo"
      description: "Tipo de activo por identificador\n"
      operationId: "atIdGet"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de activo."
        required: true
        type: "string"
      responses:
        200:
          description: "Tipo de activo por id."
          schema:
            $ref: "#/definitions/AssetType"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    put:
      tags:
        - "AssetType"
      summary: "Actualizacion de un tipo de activo informático"
      description: "Actualiza los datos asociados a un tipo de activo informático\n"
      operationId: "atIdPut"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de activo."
        required: true
        type: "string"
      - in: "body"
        name: "assetType"
        description: "Nuevos datos del activo"
        required: true
        schema:
          $ref: "#/definitions/UpdateAssetType"
      responses:
        200:
          description: "Activo actualizado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"

  /assetTypes/{id}/LifeCycle/Graph:
    x-swagger-router-controller: "AssetType"
    get:
      tags:
      - "AssetType"
      summary: "Imagen de ciclo de vida"
      description: "Imagen del ciclo de vida asociado al tipo de activo solicitado\n"
      operationId: "graphAtIdGet"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de activo."
        required: true
        type: "string"
      responses:
        200:
          description: "Imagen de ciclo de vida"
          schema:
            $ref: "#/definitions/LifeCycleGraph"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assets:
    x-swagger-router-controller: "Asset"
    get:
      tags:
        - "Asset"
      summary: "Consulta de activos"
      description: "Listado de todos los activos registrados en el sistema"
      operationId: "assetsGet"
      parameters: []
      responses:
        200:
          description: "Listado de activos"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/Asset"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    post:
      tags:
        - "Asset"
      summary: "Registrar un nuevo activo"
      description: "Dar de alta un nuevo activo en el sistema"
      operationId: "assetsPost"
      parameters:
        - in: "body"
          name: "asset"
          description: "Activo informático a regristrar en el sistema"
          required: true
          schema:
            $ref: "#/definitions/NewAsset"
      responses:
        201:
          description: "Activo creado"
          schema:
            $ref: "#/definitions/NewResource"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assetTypes/{typeId}/assets:
    x-swagger-router-controller: "Asset"
    get:
      tags:
        - "Asset"
      summary: "Consulta de activos informáticos por tipo"
      description: "Listado de todos los activos informáticos pertenecientes al tipo provisto"
      operationId: "assetsGetByType"
      parameters:
      - name: "typeId"
        in: "path"
        description: "tipo del activo."
        required: true
        type: "string"
      responses:
        200:
          description: "Listado de activos"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssetByType"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assets/{id}:
    x-swagger-router-controller: "Asset"
    get:
      tags:
        - "Asset"
      description: "Activo por identificador\n"
      operationId: "assetsIdGet"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del activo."
        required: true
        type: "string"
      responses:
        200:
          description: "Activo por id."
          schema:
            $ref: "#/definitions/Asset"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    put:
      tags:
        - "Asset"
      summary: "Actualizacion de un activo informático"
      description: "Actualiza los datos asociados a un activo informático\n"
      operationId: "assetIdPut"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del activo."
        required: true
        type: "string"
      - in: "body"
        name: "asset"
        description: "Nuevos datos del activo"
        required: true
        schema:
          $ref: "#/definitions/UpdateAsset"
      responses:
        200:
          description: "Activo actualizado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    delete:
      tags:
        - "Asset"
      summary: "Eliminar activo"
      description: "Elimina lógicamente un activo"
      operationId: "assetIdDelete"
      parameters:
        - name: "id"
          in: "path"
          description: "identificador del activo."
          required: true
          type: "string"
      responses:
        200:
          description: "Activo borrado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"

  /assets/{id}/LifeCycle/Graph:
    x-swagger-router-controller: "Asset"
    get:
      tags:
      - "Asset"
      summary: "Imagen de ciclo de vida marcando estado actual"
      description: "Imagen del ciclo de vida asociado al activo solicitado indicando el estado actual\n"
      operationId: "graphIdGet"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de activo."
        required: true
        type: "string"
      responses:
        200:
          description: "Imagen de ciclo de vida"
          schema:
            $ref: "#/definitions/LifeCycleGraph"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assets/{id}/relations:
    x-swagger-router-controller: "Relation"
    get:
      tags:
        - "Relation"
      description: "Listado de relaciones hacia otros activos"
      operationId: "relationGet"
      parameters:
        - name: "id"
          in: "path"
          description: "identifcador del activo del cual se quieren conocer las relaciones"
          required: true
          type: "string"
      responses:
        200:
          description: "Listado de relaciones"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/Relation"
        404:
          description: "Activo no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    post:
      tags:
        - "Relation"
      description: "Registrar una nueva relacion"
      operationId: "relationPost"
      parameters:
        - name: "id"
          in: "path"
          description: "identifcador del activo del cual se quieren conocer las relaciones"
          required: true
          type: "string"
        - in: "body"
          name: "relation"
          description: "Relacion a registrar en el sistema"
          required: true
          schema:
            $ref: "#/definitions/NewRelation"
      responses:
        201:
          description: "Relacion creada"
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Activo no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assets/{id}/relationsTree:
    x-swagger-router-controller: "Relation"
    get:
      tags:
        - "Relation"
      description: "Arbol de relaciones a partir de un activo dado"
      operationId: "relationsTreeGet"
      parameters:
        - name: "id"
          in: "path"
          description: "identifcador del activo del cual se quieren conocer las relaciones"
          required: true
          type: "string"
      responses:
        200:
          description: "Arbol de activos relacionados"
          schema:
            $ref: "#/definitions/AssetTree"
        404:
          description: "Activo no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assets/{id}/relations/incoming:
    x-swagger-router-controller: "Relation"
    get:
      tags:
        - "Relation"
      description: "Listado de relaciones desde otros activos"
      operationId: "counterRelationGet"
      parameters:
        - name: "id"
          in: "path"
          description: "identifcador del activo del cual se quieren conocer las relaciones entrantes"
          required: true
          type: "string"
      responses:
        200:
          description: "Listado de relaciones entrantes"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/Relation"
        404:
          description: "Activo no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /assets/{id}/relations/{relId}:
    x-swagger-router-controller: "Relation"
    get:
      tags:
        - "Relation"
      description: "Relacion por identificador"
      operationId: "relationIdGet"
      parameters:
        - name: "id"
          in: "path"
          description: "identificador del activo"
          required: true
          type: "string"
        - name: "relId"
          in: "path"
          description: "identificador del activo con el cual se relaciona"
          required: true
          type: "string"
      responses:
        200:
          description: "Relacion por id"
          schema:
            $ref: "#/definitions/Relation"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    put:
      tags:
        - "Relation"
      description: "Actualizar datos de relacion"
      operationId: "relationPut"
      parameters:
        - name: "id"
          in: "path"
          description: "identificador del activo"
          required: true
          type: "string"
        - name: "relId"
          in: "path"
          description: "identificador de la relacion"
          required: true
          type: "string"
        - in: "body"
          name: "relation"
          description: "Nuevos datos de la relacion"
          required: true
          schema:
            $ref: "#/definitions/UpdateRelation"
      responses:
        200:
          description: "Relacion actualizada"
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    delete:
      tags:
        - "Relation"
      description: "Elimina lógicamente una relación"
      operationId: "relationDelete"
      parameters:
        - name: "id"
          in: "path"
          description: "identificador del activo"
          required: true
          type: "string"
        - name: "relId"
          in: "path"
          description: "identificador del activo con el cual se relaciona"
          required: true
          type: "string"
      responses:
        200:
          description: "Relacion borrada."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /relationTypes:
    x-swagger-router-controller: "RelationType"
    get:
      tags:
      - "RelationType"
      summary: "Consulta de tipos de relación"
      description: "Listado de todos los tipos de relación"
      operationId: "relTypeGet"
      parameters: []
      responses:
        200:
          description: "Listado de tipos de relación"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/RelationType"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    post:
      tags:
      - "RelationType"
      summary: "Crear tipo de relación"
      description: "Dar de alta un tipo de relación"
      operationId: "relTypePost"
      parameters:
      - in: "body"
        name: "relType"
        description: "Tipo de relación a dar de alta en el sistema"
        required: true
        schema:
          $ref: "#/definitions/NewRelationType"
      responses:
        201:
          description: "Tipo de relación creado"
          schema:
            # a pointer to a definition
            $ref: "#/definitions/NewResource"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
        403:
          description: "El tipo de activo ya existe"
          schema:
            $ref: "#/definitions/Notification"
  /relationTypes/{id}:
    x-swagger-router-controller: "RelationType"
    get:
      tags:
      - "RelationType"
      summary: "Tipo de relación"
      description: "Tipo de relación por identificador\n"
      operationId: "relTypeIdGet"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de relación."
        required: true
        type: "string"
      responses:
        200:
          description: "Tipo de relación por id."
          schema:
            $ref: "#/definitions/RelationType"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    put:
      tags:
      - "RelationType"
      summary: "Tipo de relación"
      description: "Tipo de relación por identificador\n"
      operationId: "relTypeIdPut"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de relación."
        required: true
        type: "string"
      - in: "body"
        name: "relType"
        description: "Datos del tipo de relación para actualizar"
        required: true
        schema:
          $ref: "#/definitions/UpdateRelationType"
      responses:
        200:
          description: "Tipo de relación actualizado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
    delete:
      tags:
      - "RelationType"
      summary: "Tipo de relación"
      description: "Tipo de relación por identificador\n"
      operationId: "relTypeIdDelete"
      parameters:
      - name: "id"
        in: "path"
        description: "identificador del tipo de relación."
        required: true
        type: "string"
      responses:
        200:
          description: "Tipo de relación borrado."
          schema:
            $ref: "#/definitions/Notification"
        404:
          description: "Recurso no encontrado"
          schema:
            $ref: "#/definitions/Notification"
        500:
          description: "Error interno del servidor"
          schema:
            $ref: "#/definitions/Notification"
  /swagger:
    x-swagger-pipe: swagger_raw
# complex objects have schema definitions
definitions:
  User:
    type: "object"
    required:
      - _id
      - name
    properties:
      _id:
        type: "string"
        description: "identificador único del usuario."
      name:
        type: "string"
        description: "Nombre de usuario."
      comment:
        type: "string"
        description: "Comentario acerca del usuario."
      deleted:
        type: "boolean"
        description: "Indica si se trata de un usuario ya eliminado o no."
  UpdateUser:
    required:
      - comment
    type: "object"
    properties:
      comment:
        type: "string"
        description: "Comentario acerca del usuario."
  NewUser:
    type: "object"
    required:
      - name
    properties:
      name:
        type: "string"
        description: "Nombre de usuario."
      comment:
        type: "string"
        description: "Comentario acerca del usuario."
  RelationType:
    type: "object"
    required:
      - _id
    properties:
      _id:
        type: "string"
        description: "identificador único del tipo de relación."
      inLabel:
        type: "string"
        description: "Descripción del tipo de relación (saliente)."
      outLabel:
        type: "string"
        description: "Descripción del tipo de relación (entrante)."
      comment:
        type: "string"
        description: "Comentario acerca del tipo de relación."
      deleted:
        type: "boolean"
        description: "Indica si se trata de un tipo de relación ya eliminado o no."
  UpdateRelationType:
    type: "object"
    properties:
      comment:
        type: "string"
        description: "Comentario acerca del tipo de relación."
      inLabel:
        type: "string"
        description: "Descripción del tipo de relación (saliente)."
      outLabel:
        type: "string"
        description: "Descripción del tipo de relación (entrante)."
  NewRelationType:
    type: "object"
    required:
      - _id
    properties:
      _id:
        type: "string"
        description: "Nombre del tipo de relación."
      inLabel:
        type: "string"
        description: "Descripción del tipo de relación (saliente)."
      outLabel:
        type: "string"
        description: "Descripción del tipo de relación (entrante)."
      comment:
        type: "string"
        description: "Comentario acerca del usuario."
  AssetType:
    type: "object"
    required:
      - _id
      - name
      - properties
      - lifeCycle
    properties:
      _id:
        type: "string"
        description: "identificador único del tipo de activo."
      name:
        type: "string"
        description: "Nombre del tipo de activo."
      comment:
        type: "string"
        description: "Comentario acerca del tipo de activo."
      properties:
        type: "array"
        description: "Lista de propiedades del tipo de activo."
        items:
          $ref: "#/definitions/Property"
      lifeCycle:
        type: "array"
        description: "Lista de todos los nodos del ciclo de vida del tipo de activo."
        items:
          $ref: "#/definitions/LifeCycle"
  LifeCycle:
    type: "object"
    required:
      - name
      - isInitial
      - isFinal
      - adjacents
    properties:
      name:
        type: "string"
        description: "Nombre del estado. Este debe ser único entre todos los nodos del ciclo de vida."
      isInitial:
        type: "boolean"
        description: "Indica si el nodo es inicial. Sólo puede existir un nodo inicial en un ciclo de vida."
      isFinal:
        type: "boolean"
        description: "Indica si el nodo es final. Pueden existir múltiples nodos finales en un ciclo de vida."
      adjacents:
        type: "array"
        items:
          type: "string"
        description: "listado de nombres de nodos adyacentes"
      comment:
        type: "string"
        description: "Comentario acerca del estado"
  UpdateAssetType:
    type: "object"
    required:
      - name
      - properties
      - lifeCycle
    properties:
      name:
        type: "string"
        description: "Nombre del tipo de activo."
      comment:
        type: "string"
        description: "Comentario acerca del tipo de activo."
      properties:
        type: "array"
        description: "Lista de propiedades del tipo de activo."
        items:
          $ref: "#/definitions/Property"
      lifeCycle:
        type: "array"
        description: "Lista de todos los nodos del ciclo de vida del tipo de activo."
        items:
          $ref: "#/definitions/LifeCycle"
  Property:
    type: "object"
    required:
      - name
      - type
      - required
    properties:
      name:
        type: "string"
        description: nombre de la propiedad del activo
      type:
        type: "string"
        description: tipo de dato de la propiedad
      required:
        type: "boolean"
        description: indica si la propiedad es requerida o no
      comment:
        type: "string"
        description: breve descripcion acerca de la propiedad
  Asset:
    type: "object"
    properties:
      typeId:
        type: "string"
        required:
          - typeId
          # - estadoActual
        description: id del tipo de activo
      id:
        type: "string"
        description: "identificador único"
      deleted:
        type: "boolean"
        description: "indica si el activo se encuentra borrado"
      estadoActual:
        type: "string"
        description: nombre del estado actual del activo en su ciclo de vida
      anyProperty:
        type: ["string", "integer", "number", "boolean"]
        description: "Propiedad y valor. Pueden agregarse cuantas propiedades sean necesarias y del tipo que sean necesarias."
  AssetTree:
    type: "object"
    properties:
      id:
        type: "string"
        description: "identificador único"
      deleted:
        type: "boolean"
        description: "indica si el activo se encuentra borrado"
      estadoActual:
        type: "string"
        description: nombre del estado actual del activo en su ciclo de vida
      anyProperty:
        type: ["string", "integer", "number", "boolean"]
        description: "Propiedad y valor. Pueden agregarse cuantas propiedades sean necesarias y del tipo que sean necesarias."
  UpdateAsset:
    type: "object"
    properties:
      anyProperty:
        type: ["string", "integer", "number", "boolean"]
        description: "Propiedad y valor. Pueden agregarse cuantas propiedades sean necesarias y del tipo que sean necesarias."
  AssetByType:
    type: "object"
    properties:
      id:
        type: "string"
        description: "identificador único"
      anyProperty:
        type: ["string", "integer", "number", "boolean"]
        description: "Propiedad y valor. Pueden agregarse cuantas propiedades sean necesarias y del tipo que sean necesarias."
  Relation:
    type: "object"
    required:
      - id
      - relatedAssetId
      - relationTypeId
    properties:
      id:
        type: "string"
        description: "identificador único de la relación"
      isCritical:
        type: "boolean"
        description: "indica una relación de dependencia fuerte entre dos activos"
      comment:
        type: "string"
        description: "comentario acerca de la relación"
      relatedAssetId:
        type: "string"
        description: "id del activo relacionado"
      relationTypeId:
        type: "string"
        description: "id del tipo de relación"
  NewRelation:
    type: "object"
    required:
      - relatedAssetId
      - relationTypeId
    properties:
      isCritical:
        type: "boolean"
        description: "indica una relación de dependencia fuerte entre dos activos"
      comment:
        type: "string"
        description: "comentario acerca de la relación"
      relatedAssetId:
        type: "string"
        description: "id del activo relacionado"
      relationTypeId:
        type: "string"
        description: "id del tipo de relación"
  UpdateRelation:
    type: "object"
    properties:
      isCritical:
        type: "boolean"
        description: "indica una relación de dependencia fuerte entre dos activos"
      comment:
        type: "string"
        description: "comentario acerca de la relación"
  NewAsset:
    type: "object"
    required:
      - typeId
      # - estadoActual
    properties:
      typeId:
        type: "string"
        description: id del tipo de activo
      estadoActual:
        type: "string"
        description: nombre del estado actual del activo en su ciclo de vida
      anyProperty:
        type: ["string", "integer", "number", "boolean"]
        description: "Propiedad y valor. Pueden agregarse cuantas propiedades sean necesarias y del tipo que sean necesarias."
  NewAssetType:
    type: "object"
    required:
      - name
      - properties
    properties:
      name:
        type: "string"
        description: "Nombre del tipo de activo."
      comment:
        type: "string"
        description: "Comentario acerca del tipo de activo."
      properties:
        type: "array"
        description: "Lista de propiedades del tipo de activo."
        items:
          $ref: "#/definitions/Property"
      lifeCycle:
        type: "array"
        description: "Lista de todos los nodos del ciclo de vida del tipo de activo."
        items:
          $ref: "#/definitions/LifeCycle"
  NewResource:
    type: "object"
    required:
      - id
    properties:
      id:
        type: string
  LifeCycleGraph:
    type: "object"
    properties:
      img:
        type: "string"
  Notification:
    type: "object"
    properties:
      code:
        type: "integer"
        format: "int32"
      message:
        type: "string"
